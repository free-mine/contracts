{
  "type": "module",
  "scripts": {
    "deploy": "node src/deploy",
    "build": "hardhat build --build-profile production && node src/prepare"
  },
  "devDependencies": {
    "hardhat": "3.12.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.6.1",
    "dotenv": "^17.4.2",
    "ethers": "^6.17.0"
  }
}
